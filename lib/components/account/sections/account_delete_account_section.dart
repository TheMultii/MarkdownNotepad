import 'package:flutter/material.dart';

class AccountDeleteAccountSection extends StatelessWidget {
  const AccountDeleteAccountSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
